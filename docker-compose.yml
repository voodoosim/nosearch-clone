services:
  nextjs:
    build: .
    ports:
      - "127.0.0.1:3000:3000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - PB_URL=http://host.docker.internal:8090
      - AUTH_SECRET=smartdeal-secret-key-2026
    restart: unless-stopped
